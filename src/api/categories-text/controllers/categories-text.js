'use strict';

/**
 * categories-text controller
 */

const { createCoreController } = require('@strapi/strapi').factories;

module.exports = createCoreController('api::categories-text.categories-text');
